name: Test bootstrap PR 34 spec-path repo sync

on:
  workflow_dispatch:
    inputs:
      spec_path:
        description: Repo-root-relative local OpenAPI spec path.
        required: true
        default: openapi/dummy-openapi.yaml
      collection_sync_mode:
        description: Collection refresh behavior.
        required: true
        default: refresh
        type: choice
        options:
          - refresh
          - version
      spec_sync_mode:
        description: Spec refresh behavior.
        required: true
        default: update
        type: choice
        options:
          - update
          - version

jobs:
  onboarding:
    runs-on: ubuntu-latest
    permissions:
      actions: write
      contents: write
    env:
      BOOTSTRAP_REF: 3d44b113c1599b2b9f3ceac5ff10be72f2ca64ed
    steps:
      - name: Checkout test repo
        uses: actions/checkout@v4

      - name: Verify local OpenAPI spec
        run: test -f "${{ inputs.spec_path }}"

      - name: Checkout onboarding action
        uses: actions/checkout@v4
        with:
          repository: postman-cs/postman-api-onboarding-action
          ref: main
          path: .github/actions/postman-api-onboarding-action

      - name: Exclude local action checkout from repo sync commits
        run: echo ".github/actions/postman-api-onboarding-action/" >> .git/info/exclude

      - name: Point onboarding at bootstrap PR 34 spec-path
        run: |
          node <<'NODE'
          const fs = require('node:fs');
          const actionPath = '.github/actions/postman-api-onboarding-action/action.yml';
          const bootstrapRef = process.env.BOOTSTRAP_REF;
          let source = fs.readFileSync(actionPath, 'utf8');

          const bootstrapBefore = 'postman-cs/postman-bootstrap-action@v0.13.0';
          const bootstrapAfter = `postman-cs/postman-bootstrap-action@${bootstrapRef}`;
          if (!source.includes(bootstrapBefore)) {
            throw new Error(`Could not find ${bootstrapBefore} in ${actionPath}`);
          }
          source = source.replace(bootstrapBefore, bootstrapAfter);

          const beforeSpecUrlRequired = source;
          source = source.replace(
            /(\n  spec-url:\n    description: URL to the OpenAPI document to bootstrap\.\n    required: )true/,
            '$1false'
          );
          if (source === beforeSpecUrlRequired) {
            throw new Error(`Could not mark spec-url optional in ${actionPath}`);
          }

          if (!source.includes('\n  spec-path:\n')) {
            throw new Error(`The checked-out onboarding action does not expose a top-level spec-path input`);
          }

          const bootstrapSpecUrl = '        spec-url: $' + '{{ inputs.spec-url }}';
          const bootstrapSpecPath = '        spec-path: $' + '{{ inputs.spec-path }}';
          if (!source.includes(`${bootstrapSpecUrl}\n${bootstrapSpecPath}`)) {
            if (!source.includes(bootstrapSpecUrl)) {
              throw new Error(`Could not find bootstrap spec-url input in ${actionPath}`);
            }
            source = source.replace(bootstrapSpecUrl, `${bootstrapSpecUrl}\n${bootstrapSpecPath}`);
          }

          fs.writeFileSync(actionPath, source);
          console.log(`Using ${bootstrapAfter} with spec-path passed through to bootstrap`);
          NODE

      - name: Show patched spec-path wiring
        run: |
          grep -nE "spec-url|spec-path|postman-bootstrap-action@" .github/actions/postman-api-onboarding-action/action.yml

      - id: onboarding
        name: Run onboarding flow with local spec-path
        uses: ./.github/actions/postman-api-onboarding-action
        with:
          project-name: pr-merge-test-pr34-${{ github.run_id }}
          spec-url: ""
          spec-path: ${{ inputs.spec_path }}
          collection-sync-mode: ${{ inputs.collection_sync_mode }}
          spec-sync-mode: ${{ inputs.spec_sync_mode }}
          postman-api-key: ${{ secrets.POSTMAN_API_KEY }}
          postman-access-token: ${{ secrets.POSTMAN_ACCESS_TOKEN }}
          github-token: ${{ secrets.GITHUB_TOKEN }}
          gh-fallback-token: ${{ secrets.GH_FALLBACK_TOKEN }}
          repo-write-mode: commit-and-push
          generate-ci-workflow: "false"
          environments-json: '["prod"]'
          env-runtime-urls-json: '{"prod":"https://api.example.com"}'

      - name: Print onboarding outputs
        if: always()
        run: |
          echo "bootstrap-outcome=${{ steps.onboarding.outputs.bootstrap-outcome }}"
          echo "repo-sync-outcome=${{ steps.onboarding.outputs.repo-sync-outcome }}"
          echo "workspace-id=${{ steps.onboarding.outputs.workspace-id }}"
          echo "workspace-url=${{ steps.onboarding.outputs.workspace-url }}"
          echo "spec-id=${{ steps.onboarding.outputs.spec-id }}"
          echo "commit-sha=${{ steps.onboarding.outputs.commit-sha }}"
          echo "repo-sync-summary-json=${{ steps.onboarding.outputs.repo-sync-summary-json }}"
          echo "collection-sync-mode=${{ inputs.collection_sync_mode }}"
          echo "spec-sync-mode=${{ inputs.spec_sync_mode }}"
          git status --short

      - name: Upload generated Postman artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: postman-artifacts-bootstrap-pr34-spec-path
          path: |
            .postman/
            postman/
          if-no-files-found: ignore
